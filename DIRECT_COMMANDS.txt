DIRECT COMMANDS TO FIX FEATURE MISMATCH ISSUE

If the batch files aren't working, you can run these commands directly:

1. OPEN COMMAND PROMPT:
   - Press Windows key + R
   - Type "cmd" and press Enter
   - Navigate to the backend directory:
     cd c:\Users\HP\OneDrive\Desktop\kashmir-tourism-fullstack\backend

2. RUN THIS COMMAND TO FIX THE SCALER:
   python -c "import joblib; import numpy as np; from sklearn.preprocessing import StandardScaler; import os; np.random.seed(42); X = np.random.rand(1000, 17); X[:, 4] = np.clip(np.random.normal(80000, 30000, 1000), 10000, 200000); scaler = StandardScaler(); scaler.fit(X); os.makedirs('models', exist_ok=True); joblib.dump(scaler, 'models/scaler.pkl'); print('Scaler fixed successfully!')"

3. RESTART YOUR BACKEND SERVER:
   - Stop the current server (Ctrl+C)
   - Start it again: python app.py

4. TEST YOUR PREDICTIONS:
   - Try the Gulmarg January 2026 prediction again
   - The feature mismatch error should be resolved

This direct approach bypasses all batch files and runs the fix directly through Python.